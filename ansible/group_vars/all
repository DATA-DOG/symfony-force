# vi: set ft=yaml ts=2 sw=2 sts=2 :

# user - is current application owner
home: "/home/{{ user }}"
# name - is an application currently played, like frontend
app: "{{ home }}/{{ name }}"
releases: "{{ home }}/{{ name }}/releases"
current: "{{ home }}/{{ name }}/current"
builds: "{{ home }}/{{ name }}/builds"
shared: "{{ home }}/{{ name }}/shared"

